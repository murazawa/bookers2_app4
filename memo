git add .
git commit -m "[Update]"
git push origin main 

<%=%>
<%%>

binding調べるる


      <div id="favoites_btn_<%= @book.id %>">
        <%= render 'favorites/favorite', book: @book %>
      </div>
      非同期のやつ